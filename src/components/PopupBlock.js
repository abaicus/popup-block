class PopupBlock {
	constructor( element ) {
		this.element = element;
		this.init();
	}

	init() {
		this.bindCloseButtons();
	}

	bindCloseButtons() {
		// const { closeType } = this.element.dataset;
	}
}

export { PopupBlock };
